import { useAuth } from "../auth/authcontext"

const AdmittedStatus = ()=>{
    const auth = useAuth();
    const fetch
    return (
        <div>

        </div>
    )
}
export default AdmittedStatus